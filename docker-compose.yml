services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: movie-grabber-api
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
